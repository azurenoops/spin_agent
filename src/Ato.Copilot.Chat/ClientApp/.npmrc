# react-scripts@5.0.1 declares an optional peer dep on typescript@^3||^4.
# TypeScript 5.x satisfies the underlying toolchain (fork-ts-checker >=2.7)
# but npm strict mode rejects the optional peer range mismatch.
# legacy-peer-deps allows the install to succeed without downgrading TS.
legacy-peer-deps=true
