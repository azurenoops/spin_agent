/**
 * Epic #207 / Task #235 — Production build stub.
 *
 * This file is aliased in place of SimulationPanel.tsx by vite.config.ts
 * when NODE_ENV=production. It exports a null default so the
 * `SimulationPanel !== null` guard in LoginPage.tsx resolves to false at
 * compile time and the entire conditional is eliminated by Rollup.
 *
 * The stub contains no simulation-mode identifiers, ensuring the FedRAMP
 * constraint ("simulation code absent from production bundle") is satisfied
 * even if Rollup emits this file as a chunk.
 *
 * DO NOT add simulation-related imports, strings, or logic here.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export default null as any;
